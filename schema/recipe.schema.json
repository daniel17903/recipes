{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://daniel17903.github.io/recipes/schema/recipe.schema.json",
  "title": "Rezept",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "title", "category", "yield", "ingredients", "steps", "dateAdded"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "URL-tauglicher Slug, eindeutig, entspricht dem Dateinamen."
    },
    "title": { "type": "string", "minLength": 1 },
    "category": { "type": "string", "minLength": 1 },
    "yield": { "$ref": "#/$defs/yield" },
    "times": { "$ref": "#/$defs/times" },
    "ingredients": { "$ref": "#/$defs/ingredients" },
    "steps": { "$ref": "#/$defs/steps" },
    "notes": { "type": "string" },
    "nutrition": { "type": "string" },
    "source": { "$ref": "#/$defs/source" },
    "images": {
      "type": "array",
      "items": { "type": "string" }
    },
    "dateAdded": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "variations": {
      "type": "array",
      "minItems": 1,
      "description": "Varianten des Rezepts. Jede Variante überschreibt komplette Felder des Basisrezepts; nicht angegebene Felder werden geerbt.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "title"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "description": "Slug der Variante, eindeutig innerhalb des Rezepts."
          },
          "title": { "type": "string", "minLength": 1 },
          "description": { "type": "string" },
          "yield": { "$ref": "#/$defs/yield" },
          "times": { "$ref": "#/$defs/times" },
          "ingredients": { "$ref": "#/$defs/ingredients" },
          "steps": { "$ref": "#/$defs/steps" },
          "notes": { "type": "string" },
          "source": { "$ref": "#/$defs/source" }
        }
      }
    }
  },
  "$defs": {
    "yield": {
      "type": "object",
      "additionalProperties": false,
      "required": ["amount", "unit"],
      "properties": {
        "amount": { "type": "number", "exclusiveMinimum": 0 },
        "unit": { "type": "string", "minLength": 1 }
      }
    },
    "times": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "prep": { "type": "string" },
        "cook": { "type": "string" }
      }
    },
    "ingredients": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["group", "amount", "unit", "name"],
        "properties": {
          "group": { "type": ["string", "null"] },
          "amount": { "type": ["number", "null"] },
          "unit": {
            "type": ["string", "null"],
            "enum": ["g", "ml", "Stück", "Pck.", "Prise", "Msp.", "Blatt", "Zehe", "Bund", "Dose", "Glas", null]
          },
          "name": { "type": "string", "minLength": 1 },
          "note": { "type": "string" }
        }
      }
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "url": { "type": "string" }
      }
    }
  }
}
